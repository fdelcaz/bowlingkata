public class FrameWithNextFrame {
  public FrameWithNextFrame(Integer currentScore, Integer nextScore) {
    this.nextScore = nextScore;
    this.currentScore = currentScore;
  }

  Integer currentScore;
  Integer nextScore;
}
